## 目标
- 在上传弹窗中，将“考核类型”表单项显示在“指标名称”之前，保持先选类型、后选指标的联动逻辑不变。

## 前端改动
- 调整 Dashboard.vue 上传对话框中两个 <el-form-item> 的顺序：先渲染“考核类型”，再渲染“指标名称”。
- 保持现有逻辑：未选择类型时禁用指标下拉；选择类型后通过接口加载指标列表。

## 验证
- 打开上传弹窗，确认显示顺序已切换；选择类型后指标下拉启用并加载候选项；提交仍成功。