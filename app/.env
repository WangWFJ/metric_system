# 新增 .env.example 文件内容（放到项目根目录 data_proj_v1/app/.env.example）

# ========== 基础配置 ==========
PROJECT_NAME=data_proj_v1
VERSION=0.1.0

# ========== 数据库配置（MySQL）==========
# 记得替换用户名/密码/数据库名
DATABASE_URL=mysql+aiomysql://root:123456@127.0.0.1:3306/test?charset=utf8mb4

# ========== JWT 配置 ==========
# 会自动生成默认值，也可以自定义
JWT_SECRET_KEY=changeme_secret_key
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ========== CORS 允许的来源 ==========
# 支持用逗号分隔多个域名，例如：
# CORS_ALLOW_ORIGINS=https://admin.example.com,https://api.example.com
CORS_ALLOW_ORIGINS=*


# 在使用前，复制为 .env：
# cp .env.example .env