# ================= Production .env 示例 =================
# 外部数据库连接（生产务必替换为真实账号密码）
# 例：mysql+aiomysql://user:password@10.0.0.10:3306/app_db?charset=utf8mb4
DATABASE_URL=mysql+aiomysql://root:123456@localhost:3306/app_db?charset=utf8mb4

# 后端服务端口（宿主机与容器一致），默认 8081
BACKEND_PORT=8081

# Gunicorn worker 数量（可根据 CPU 核心数调整）
WORKERS=4

# 前端对外暴露端口（宿主机），默认 8080（禁止使用 8000）
FRONTEND_PORT=8080

# Nginx 容器内监听端口（通过 envsubst 注入），默认 80
FRONTEND_CONTAINER_PORT=80

# 可选：ASGI 应用模块（默认 main:app）
#APP_MODULE=main:app

# 构建模式：在 WSL2 + Docker Desktop 环境建议禁用 BuildKit（0），如需开启设为 1
USE_BUILDKIT=0
